filter eventSource = "bedrock.amazonaws.com"
       and ispresent(additionalEventData.inferenceRegion)
| stats count(*) as calls
        by requestParameters.modelId, additionalEventData.inferenceRegion
| sort calls desc
