# Cognito user the CLI signs in as when invoking the runtime.
COGNITO_USERNAME=user1
TEST_USER_PASSWORD=<<Enter TEST_USER_PASSWORD here>>

# Optional overrides (defaults shown):
# STACK_NAME=SlackGatewayStack
# AWS_REGION=us-east-1
