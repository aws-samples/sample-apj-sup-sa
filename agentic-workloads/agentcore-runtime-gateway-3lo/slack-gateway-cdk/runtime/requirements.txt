# Generated from requirements.in — do not edit directly.
# To upgrade: edit requirements.in, then run:
#   pip-compile requirements.in -o requirements.txt
# or:
#   uv pip compile requirements.in -o requirements.txt
#
# Note: only direct dependencies are pinned here (no local venv to resolve
# transitive deps from). For a fully locked transitive graph, run
# pip-compile or uv pip compile inside the container build environment.

strands-agents==1.54.0
mcp==2.1.1
bedrock-agentcore==1.22.0
boto3==1.43.82
aws-opentelemetry-distro==0.19.0
