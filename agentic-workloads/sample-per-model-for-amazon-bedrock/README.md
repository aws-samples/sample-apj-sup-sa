## ➡️ Moved

These notebooks now live in their own repository:

**https://github.com/aws-samples/sample-per-model-bedrock**

They are also mounted in this repository as a git submodule, at
[`../sample-per-model-bedrock`](../sample-per-model-bedrock). To fetch the content
when you clone:

```bash
git clone --recurse-submodules https://github.com/aws-samples/sample-apj-sup-sa.git

# already cloned?
git submodule update --init --recursive
```

Nothing was removed — the same 33 notebooks across 16 model families are in the new
repository, which is now where they are maintained. This folder is kept only so that
links already shared to this path keep resolving.
