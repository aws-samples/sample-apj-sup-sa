[project]
name = "daf-orchestrator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "temporalio>=1.9.0",
    "httpx>=0.27.0",
    "boto3>=1.35.0",
    "a2a-sdk>=1.0.0",
    "langfuse>=2.0.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]
