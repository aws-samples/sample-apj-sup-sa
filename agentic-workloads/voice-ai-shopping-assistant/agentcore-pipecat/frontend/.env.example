# Backend /start endpoint (API Gateway, NOT a Lambda Function URL — see Sev2 note).
# POST here returns { "room_url": "...", "status": "ok" } and launches the bot.
VITE_START_URL=https://l5uf3gixrj.execute-api.ap-southeast-2.amazonaws.com/prod
